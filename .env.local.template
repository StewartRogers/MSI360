# Enable AI features (routes requests to /api/gemini proxy)
VITE_GEMINI_ENABLED=true

# Server-side env vars (set these in Vercel project settings for production):
# GEMINI_API_KEY=your_real_key_here
# GEMINI_MODEL=gemini-3.1-flash-lite
# ALLOWED_ORIGINS=https://your-domain.vercel.app
# RESEND_API_KEY=your_real_key_here
# RESEND_FROM_EMAIL="Prototype <onboarding@resend.dev>"  # switch to a verified domain address in production
