# For local dev: set VITE_GEMINI_API_KEY so the client knows AI is available.
# The actual key is used server-side only (see api/gemini.ts).
VITE_GEMINI_API_KEY=1

# Server-side env vars (set these in Vercel project settings for production):
# GEMINI_API_KEY=your_real_key_here
# GEMINI_MODEL=gemini-2.5-flash