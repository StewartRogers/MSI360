/**
 * Public catalog barrel for survey data.
 *
 * Import from this file when application logic needs question structure,
 * language metadata, section ordering, risk factors, or the routing tag
 * taxonomy. Text copy remains in `src/data/translations`.
 */
export { languages } from "./languages";
export { questions } from "./questions";
export { sectionOrder } from "./sections";
export { riskFactors, tagTaxonomy } from "./tags";
