import type { Translation } from "../../types";
import { en } from "./en";

// Placeholder translation file. Replace this with the complete hard-coded Ilocano question text later.
export const ilo: Translation = en;
