import type { Translation } from "../../types";
import { en } from "./en";

// Placeholder translation file. Replace this with the complete hard-coded Vietnamese question text later.
export const vi: Translation = en;
