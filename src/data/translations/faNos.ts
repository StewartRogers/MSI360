import type { Translation } from "../../types";
import { en } from "./en";

// Placeholder translation file. Replace this with the complete hard-coded Persian (Farsi), n.o.s. question text later.
export const faNos: Translation = en;
