import type { Translation } from "../../types";
import { en } from "./en";

// Placeholder translation file. Replace this with the complete hard-coded Urdu question text later.
export const ur: Translation = en;
