import type { Translation } from "../../types";
import { en } from "./en";

// Placeholder translation file. Replace this with the complete hard-coded Hindi question text later.
export const hi: Translation = en;
