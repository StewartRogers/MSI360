import type { Translation } from "../../types";
import { en } from "./en";

// Placeholder translation file. Replace this with the complete hard-coded German question text later.
export const de: Translation = en;
