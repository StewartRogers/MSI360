import type { Translation } from "../../types";
import { en } from "./en";

// Placeholder translation file. Replace this with the complete hard-coded Italian question text later.
export const it: Translation = en;
